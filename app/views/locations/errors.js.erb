<% if @location.errors.any? %>
  $('#errors').html("<%= j render 'locations/errors', location: @location %>")
  $("#editerrors").html("<%= j(render 'locations/errors', location: @location) %>");
<% end %>