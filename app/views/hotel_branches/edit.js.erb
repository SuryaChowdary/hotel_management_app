$(document).ready(function() {
  $('#editModal').modal('show');
  $('#form-container').html('<%= escape_javascript(render 'hotel_branches/form') %>');
});

function location_reload() {
    window.location.reload();
  }
