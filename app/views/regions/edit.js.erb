$(document).ready(function() {
  $('#editModal').modal('show');
  $('#form-container').html('<%= escape_javascript(render 'regions/form') %>');
});

function region_reload() {
    window.location.reload();
  }
